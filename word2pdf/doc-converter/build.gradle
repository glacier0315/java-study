dependencies {
    implementation fileTree(dir:'lib',include:['*.jar'])

    implementation (
            "org.jodconverter:jodconverter-core:${jodconverterCoreVersion}",
            "net.sf.jacob-project:jacob:${jacobVersion}"
    )
}
