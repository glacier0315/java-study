/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.glacier.app;

import com.glacier.list.LinkedList;

import static com.glacier.utilities.StringUtils.join;
import static com.glacier.utilities.StringUtils.split;
import static com.glacier.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
